'use strict';

module.exports.testDataDrivenDBInputFormat = function (cb) {

  try {
    require('../DataDrivenDBInputFormatTests')
        .testDataDrivenDBInputFormat(require('hadoop-input').DataDrivenDBInputFormat, cb);
  } catch (err) {
    if (err.stack) {
      console.error(err.stack);
    }
    throw new Error(err.message);
  }

  cb();

};
