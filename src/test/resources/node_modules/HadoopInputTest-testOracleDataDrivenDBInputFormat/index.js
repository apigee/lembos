'use strict';

module.exports.testOracleDataDrivenDBInputFormat = function (cb) {

  try {
    require('../DataDrivenDBInputFormatTests')
        .testDataDrivenDBInputFormat(require('hadoop-input').OracleDataDrivenDBInputFormat, cb);
  } catch (err) {
    if (err.stack) {
      console.error(err.stack);
    }
    throw new Error(err.message);
  }

  cb();

};
