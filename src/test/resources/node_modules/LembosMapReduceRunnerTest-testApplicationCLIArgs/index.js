'use strict';

var appArgs = process.argv.slice(2)
  , assert = require('assert');

assert.equal(1, appArgs.length);
assert.equal('test', appArgs[0]);

module.exports = {

  map: function (key, value, context, cb) {

    cb();

  }

};
