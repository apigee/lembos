'use strict';

module.exports.testTextOutputFormat = function (cb) {

  try {
    require('../FileOutputFormatTests').testFileOutputFormat(require('hadoop-output').TextOutputFormat);
  } catch (err) {
    if (err.stack) {
      console.error(err.stack);
    }
    throw new Error(err.message);
  }

  cb();

};