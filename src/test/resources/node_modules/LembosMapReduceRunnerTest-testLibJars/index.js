'use strict';

var lembosLibJarsTest = require('lembos-mapreduce-test-libjars')
  , className = lembosLibJarsTest.getClassName();

if (className !== '_lembos-mapreduce-test-libjars') {
  throw new Error('Unable to interact with the Java-based Node.js module as expected');
}

module.exports = {
  config: {

  },

  map: function (key, value, context, cb) {
    cb();
  }
};
