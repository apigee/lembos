'use strict';

module.exports.testSequenceFileAsBinaryInputFormat = function (cb) {

  try {
    require('../FileInputFormatTests').testFileInputFormat(require('hadoop-input').SequenceFileAsBinaryInputFormat, cb);
  } catch (err) {
    if (err.stack) {
      console.error(err.stack);
    }
    throw new Error(err.message);
  }

  cb();

};