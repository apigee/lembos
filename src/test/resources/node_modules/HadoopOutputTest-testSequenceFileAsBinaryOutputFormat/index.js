'use strict';

module.exports.testSequenceFileAsBinaryOutputFormat = function (cb) {

  try {
    require('../SequenceFileOutputFormatTests')
        .testFileOutputFormat(require('hadoop-output').SequenceFileAsBinaryOutputFormat);
  } catch (err) {
    if (err.stack) {
      console.error(err.stack);
    }
    throw new Error(err.message);
  }

  cb();

};