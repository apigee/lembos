'use strict';

module.exports = {
  config: {},

  map: function (key, value, context, cb) {
    try {
      context.write();

      cb(new Error('This should had failed'));
    } catch (err) {
      if (err.stack) {
        console.error(err.stack);
      }
      cb(err);
    }
  }
};
