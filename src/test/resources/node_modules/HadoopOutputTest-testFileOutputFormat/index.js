'use strict';

module.exports.testFileOutputFormat = function (cb) {

  try {
    require('../FileOutputFormatTests').testFileOutputFormat(require('hadoop-output').FileOutputFormat);
  } catch (err) {
    if (err.stack) {
      console.error(err.stack);
    }
    throw new Error(err.message);
  }

  cb();

};