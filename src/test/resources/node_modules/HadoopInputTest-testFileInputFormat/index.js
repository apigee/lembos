'use strict';

module.exports.testFileInputFormat = function (cb) {

  try {
    require('../FileInputFormatTests').testFileInputFormat(require('hadoop-input').FileInputFormat, cb);
  } catch (err) {
    if (err.stack) {
      console.error(err.stack);
    }
    throw new Error(err.message);
  }

  cb();

};