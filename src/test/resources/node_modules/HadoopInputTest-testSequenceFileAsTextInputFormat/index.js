'use strict';

module.exports.testSequenceFileAsTextInputFormat = function (cb) {

  try {
    require('../FileInputFormatTests').testFileInputFormat(require('hadoop-input').SequenceFileAsTextInputFormat, cb);
  } catch (err) {
    if (err.stack) {
      console.error(err.stack);
    }
    throw new Error(err.message);
  }

  cb();

};